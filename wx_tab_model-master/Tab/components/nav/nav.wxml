
<!-- Nav导航 -->
  <view
    wx:for="{{navTitle}}"
    wx:key="{{index}}"
    bindtap="handleNav"
    wx:for-index="i"
    data-tempindex="{{i}}"
  >
  <button class="btn {{item.id===navId?'active':''}}">{{item.title}} </button>
  </view>
